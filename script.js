// Javascript: Create Strings using Template Literals
console.clear();

const person = {
    name: 'VedaGna',
    age: 5
}
const greets = `Hello ${person.name}! I'm ${person.age} years old!`;
console.log(greets); // Hello VedaGna! I'm 5 years old!

