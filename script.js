// Javascript: Else If Statements
console.clear();

function testElse(val) {
    if (val > 5) {
        return "Bigger"
    } else if (val < 5) {
        return "Small"
    } else {
        return "Equal"
    }
}
console.log(testElse(5));