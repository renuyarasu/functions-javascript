// Javascript: Accessing Objects Properties with Variables
console.clear();

let testObj = {
    1: 'one',
    2: 'two',
    3: 'three',
}
let playerNum = 3;
let player = console.log(testObj[playerNum]);